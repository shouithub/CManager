# CManager v1.1.0 发布说明

发布日期：2026-08-10

本版本完整汇总 `v1.0.0` 之后的 12 个功能与修复提交，并包含本次发布资料整理提交。
重点完成全站多语言、动态内容翻译、私有文件直连与限时访问、附件预览统一，
以及首次初始化和业务输入校验加固。

## 主要更新

- 支持简体中文、英文、维吾尔文和蒙古文界面，并允许用户保存首选语言、管理员设置站点默认语言。
- 表单通道、字段、周期和示例文件支持独立译文和 UAPI 自动翻译。
- 本地私有文件通过 15 分钟签名地址访问；S3 通过短时预签名地址由客户端直接传输。
- Office、PDF、图片和其他附件使用明确且一致的预览/下载入口。
- 加固 OOBE、错误求助、验证码重发、轮播链接、导出权限、预约参数和动态数字字段。
- 修复通道编辑、slug 匹配、审核状态重算、资源 520 重试等稳定性问题。

## 全部功能与修复提交明细

### `aec8065`：搭建全站多语言基础框架

- 启用 Django 国际化中间件、语言目录、语言切换和 JavaScript 翻译路由。
- 注册中文、英文、维吾尔文、蒙古文并批量接入模板、Python 和 JavaScript 文案。
- 建立翻译目录、提取过滤器和国际化维护脚本。

### `6589d7c`：修复侧边栏语言切换入口

- 将语言按钮正确插入侧边栏底部，并与主题切换保持一致布局。
- 完成全量 102 项测试验证。

### `0a33c9f`：修复语言切换并重做入口

- 改用 Django 原生语言切换表单，解决切换不生效。
- 重做登录后语言菜单，并为未登录页面增加浮动入口。
- 填充高频译文、清理无效消息项并修复语言文件编译。

### `64425e6`：支持动态内容多语言与自动翻译

- 新增通用对象译文模型和管理员多语言编辑界面。
- 通道、字段、周期、示例文件可一键自动翻译。
- 用户端和审核端按当前语言展示，缺少译文时安全回退原文。

### `6b493bd`：增加蒙古语与维吾尔语翻译回退

- 普通翻译接口失败时自动使用 UAPI AI 翻译接口。
- 翻译服务整体不可用时静默降级，并增加回退测试。

### `cf745cd`：修复资源偶发 520 和头像异常缓存

- 样式表、头像及 Service Worker 网络请求在 5xx 时自动重试。
- 清理旧头像缓存并拒绝缓存错误响应。

### `9f885ca`：修复同名通道无法编辑

- 名称未变化时允许保存历史同名通道，真实改名时仍检查冲突。
- 修正测试数据 slug 并增加同名编辑回归测试。

### `5ec13ac`：补齐审核与审批中心翻译

- 补齐分类、通道、分页和删除确认等 Django/JavaScript 译文。
- 同步编译英文、维吾尔文和蒙古文语言文件。

### `2a7a0d0`：修复下划线 slug 错误匹配

- 优先精确匹配 URL 原始 slug，失败时才兼容旧连字符形式。
- 统一审核、审批和导出的匹配逻辑，修复译文及高亮错误。

### `6486f56`：新增用户与站点语言偏好

- 用户首选语言保存到个人档案并跨设备同步。
- 新增站点默认语言和 Django 5.2 兼容逻辑。
- 增加数据库迁移与语言优先级测试。

### `459d056`：完成翻译并集中加固安全和业务规则

- 清除三种非中文语言目录中的未翻译与模糊词条。
- 本地材料使用限时签名地址；S3 与 Office Online 使用短时直连地址。
- 限制公开媒体范围并补充安全的 Nginx 部署示例。
- OOBE 限制可信网络，SMTP 拒绝内网目标和非法端口。
- 错误求助增加签名和限速，验证码重发增加 POST 与冷却限制。
- 补充轮播链接、活动导出、预约参数、数字字段和审核状态校验。
- 增加覆盖上述行为的安全与业务回归测试。

### `34d91b6`：统一附件预览与下载

- 删除重复“查看”入口，按文件能力显示在线预览、浏览器预览或下载。
- 图片、PDF、Office、ZIP 等格式在提交详情、审核、补交和历史页保持一致。
- S3 签名明确覆盖内联/附件响应头和 MIME，避免预览再次触发下载。
- 增加 Excel 在线预览及 S3/历史附件测试，全站 150 项测试通过。

### 本次发布资料整理

- README 增加 v1.1.0 总览、逐提交中文条目、部署说明和提交规范。
- 新增本文件，为 CI 自动创建的 GitHub Release 提供完整中文正文。
- Release 任务增加代码检出步骤并读取 `body_path`，不再只展示自动生成标题。
- 发布提交和版本标签都使用中文标题及详细中文正文条目。

## 部署提示

- 部署后执行 `python manage.py migrate`，应用用户语言偏好和站点默认语言迁移。
- 执行 `python manage.py collectstatic --noinput` 更新静态资源。
- 本地存储部署请合并 `docs/deployment/nginx-media-security.conf`，不要公开映射整个 `media/`。
- S3 Bucket 建议保持私有，由系统按需签发短时直链。

## 验证情况

- Django 全站测试：150 项通过。
- Django 系统检查：通过。
- CI 同款 Ruff 致命错误检查：通过。
- 英文、维吾尔文、蒙古文翻译目录无未翻译或模糊词条。
